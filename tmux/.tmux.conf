set -g mouse on
set -g set-titles on
set -g set-titles-string "#command"
set -g status-justify centre
# fixes tmux work in iterm2
set -g default-terminal screen-256color
set-window-option -g xterm-keys on
set-option -g history-limit 10000
